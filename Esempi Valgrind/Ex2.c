#include <stdio.h>
#include <string.h> // strlen()

int main(void)
{
    char* a[3] = {"alberello","rosea","roma"};
    
    int i;
    int size=strlen(*archivio);
    
    for(i=0;i<size;i++)
        printf("%s\n",archivio[i]);
    
    return 0;
}

